# SINOQI 网站设计规范

- 版本：1.0
- 状态：已确认
- 确认日期：2026-08-08
- 适用范围：西班牙语根站、`/en/` 英语站及全部后续页面和组件
- 品牌标准写法：SINOQI（保留 S·Q 图形 Logo）
- 法律主体：Hangzhou Sinoqi Industry and Trade Co.,LTD

本文件是本项目视觉与体验实现的规范性依据。后续页面、组件、全局样式和素材处理不得自行偏离；如业务资料或品牌要求发生变化，应先更新并重新确认本规范。

## 1. 品牌与受众气质

网站应呈现为面向进口商、经销商、批发及连锁采购和工程采购客户的装饰材料 B2B 供应商网站。

品牌气质：

- 专业、稳健、务实、可验证。
- 同时体现材料质感和真实制造能力。
- 强调适合长期采购合作，不呈现为零售商城或低价批发平台。
- 真实产品、生产、仓储、包装和装柜素材优先于装饰性视觉。

禁止采用：

- 科技公司式霓虹、金属蓝或重度未来感。
- 黑金奢侈家居风格。
- 高密度目录站式布局。
- 依赖夸张数字、虚构案例、认证墙或客户 Logo 墙建立信任。
- 倒计时、连续弹窗、自动轮播等零售式转化手段。

## 2. 色彩系统

### 2.1 品牌色

| Token 建议 | 色值 | 用途 |
|---|---:|---|
| `--color-primary` | `#1F5A47` | 品牌主色、导航状态、标题和重点区块 |
| `--color-primary-dark` | `#123A2F` | Hero、Footer、深色背景和深色悬停状态 |
| `--color-primary-pale` | `#EAF1ED` | 浅色信息区、参数区、FAQ 和制造能力背景 |
| `--color-accent` | `#E69A52` | 主 CTA、选中状态和少量关键强调 |
| `--color-accent-hover` | `#D78336` | 橙色 CTA 的悬停和按下状态 |
| Logo 红色 | 使用 Logo 原始色 | 仅用于 Logo，不作为常规 UI 色 |

橙色按钮应使用深色文字，不使用白色文字。

### 2.2 中性色和状态色

| Token 建议 | 色值 | 用途 |
|---|---:|---|
| `--color-text` | `#15211D` | 主文字 |
| `--color-text-muted` | `#5F6B66` | 次级文字和说明 |
| `--color-border` | `#D7DFDA` | 边框和分隔线 |
| `--color-bg-warm` | `#F6F5F1` | 暖白背景 |
| `--color-bg` | `#FFFFFF` | 纯白背景 |
| `--color-success` | `#2E7D5B` | 成功反馈 |
| `--color-warning` | `#A85F1D` | 警告反馈 |
| `--color-error` | `#B42318` | 错误反馈 |
| `--color-info` | `#2F5F8F` | 信息提示 |

建议页面面积比例：

- 白色和暖白背景：65%–70%。
- 绿色体系：20%–25%。
- 橙色：5%–8%。
- Logo 红色：不超过 2%，且不扩展为按钮或装饰色。

## 3. 字体与字号

### 3.1 字体族

全站统一使用现代无衬线字体：

```text
Inter, "Noto Sans", "Segoe UI", Arial, sans-serif
```

- 西班牙语和英语使用同一字体体系。
- 标题、正文、按钮、数字、规格和表格均使用该字体体系。
- 不混用 Georgia、Times New Roman 或其他衬线字体。
- 不使用装饰字体。

### 3.2 字号层级

| 层级 | 桌面端 | 移动端 | 规则 |
|---|---|---|---|
| H1 | 56–64px / 1.08 | 36–42px / 1.12 | 最多约三行，避免全大写 |
| H2 | 40–48px / 1.15 | 30–36px / 1.20 | 页面模块标题 |
| H3 | 24–30px / 1.25 | 22–26px / 1.30 | 卡片或内容分组标题 |
| 大正文 | 18px / 30px | 17px / 28px | Hero 副标题、模块引言 |
| 正文 | 16px / 26px | 16px / 26px | 常规内容 |
| 说明文字 | 14px / 22px | 14px / 22px | 参数注释、图片说明 |
| 标签和元信息 | 12–13px / 18px | 12–13px / 18px | 类别、辅助信息 |
| 按钮 | 15–16px / 20px，600 | 同桌面端 | 使用正常大小写 |

正文行长建议控制在 65–75 个拉丁字符。西班牙语按钮和导航应比英语预留约 20% 的宽度。

## 4. 栅格、间距和表面规则

- 最大内容容器：1200–1240px。
- 桌面端采用 12 列栅格；平板采用 8 列；手机采用 4 列。
- 桌面左右边距：32px；平板：24px；手机：16–20px。
- 间距基准：8px。
- 模块上下间距：桌面 96–120px；平板 72–88px；手机 56–64px。
- 正文窄容器：720–800px。
- 参数表和产品矩阵宽容器：1000–1200px。
- 普通卡片、按钮和输入框圆角统一为 4px。
- 999px 圆角只用于标签、状态胶囊等小型元素。
- 普通卡片优先使用 1px 边框，不使用大面积悬浮阴影。
- 超级菜单和弹层可使用轻阴影：`0 16px 40px rgba(18, 58, 47, 0.12)`。
- 分隔线统一使用 1px `#D7DFDA`。
- Hero 不使用整屏高度；桌面建议约 620–720px，手机高度由内容决定。

## 5. 组件规范

### 5.1 Header 与导航

- 桌面 Header 主导航高度约 76–80px。
- 包含品牌 Logo、主要导航、ES/EN 切换和一个高强调询价 CTA。
- 主导航建议为：Products、Applications、Manufacturing、About、FAQ、Contact。
- Products 超级菜单只展示四条已确认产品线：PVC Ceiling、WPC Wall Panel、UV Marble Sheet、SPC Flooring。
- 超级菜单可使用小图和一句用途说明，不扩展无资料分类。
- 移动端改为抽屉和分组手风琴，不缩小成难以操作的桌面菜单。

### 5.2 按钮和链接

- 主 CTA：橙色背景、深色文字，最小高度 48px。
- 西语主 CTA：`Solicitar cotización`。
- 英语主 CTA：`Request a Custom Quote`。
- 次 CTA：绿色描边或浅绿背景，用于样品申请和查看产品。
- 样品说明必须直接注明“样品免费，客户承担快递费”。
- 文字链接使用深绿色文字和简洁箭头，并以底线、图标或其他方式提供非颜色提示。
- 同一屏幕通常只出现一个最高优先级 CTA。

### 5.3 卡片

- 产品卡图片统一使用 4:3。
- 产品名称最多两行。
- 每张卡片只提供一个主要动作。
- 普通信息卡以边框和留白区分，不依赖阴影。
- 数据必须带单位和上下文，不展示孤立的大数字。

### 5.4 表单

- 标签始终可见，不使用 placeholder 代替字段标签。
- 必填项明确标示。
- 输入框最小高度 48px，点击或触摸区域不小于 44×44px。
- 桌面可两列排列，手机必须单列。
- 错误信息在字段附近显示，并提供文字说明。
- 提交状态必须可被辅助技术读取。
- 样品、回复时效和隐私说明不得隐藏在提交完成页。

询盘表单的核心字段：姓名、公司名称、公司邮箱、国家或地区、产品、预计数量、目标交付时间、应用场景和留言。

### 5.5 表格

- 规格表优先采用字段—值结构。
- 桌面端可使用两列或多列。
- 移动端转为纵向键值卡片，尽量避免强制横向滚动。
- 长表格才使用吸顶表头。

### 5.6 FAQ

- 使用单列手风琴和 1px 分隔线。
- 整行均可展开，不只依赖小图标。
- 支持 Tab、Enter、Space 和清晰的展开状态。
- 回答正文宽度控制在约 720px 内。

### 5.7 Footer

- 使用深绿色背景。
- 信息按产品、公司、支持和联系方式分组。
- 不堆叠 SEO 关键词链接。
- 品牌名称、法律主体和联系方式必须使用已确认标准写法。

## 6. 图片与品牌素材

| 素材类型 | 主要用途 | 推荐比例 | 规则 |
|---|---|---|---|
| 产品图 | 产品卡、分类页、规格说明 | 4:3；细节可用 1:1 | 保持型材、纹理和颜色真实，不拉伸或改变表面颜色 |
| 工厂图 | Hero、Manufacturing、制造证据 | 16:9 或 3:2 | 优先真实生产、仓储、包装和装柜画面 |
| 应用图 | 首页应用、Applications | 16:9 | 效果图或非项目现场图标注为应用示意 |
| 证书图 | 首版不使用 | 原比例 | 没有独立原件时不设置认证墙或详情页 |
| 案例图 | 当前不作为案例证据 | 3:2 或 16:9 | 项目对应关系和结果不完整时，只能作为应用灵感 |
| Logo | Header、Footer、确认页 | 原比例 | 保留原始红色，不重绘、不改色、不拉伸 |

统一规则：

- 首页首屏只使用一张主视觉，不使用自动轮播或多图拼贴。
- 生产现场图保留纪实感，不进行虚假扩建或过度修饰。
- 产品色板、规格和花色编码必须可读，必要时提供局部放大。
- 不在图片上加入未经确认的客户名称、国家、认证、合作品牌或项目结果。
- 装饰性图片使用空 `alt`；产品、工厂和功能图片使用描述性 `alt`。
- 生成图不能替代工厂、生产线、交付或案例证据；生成的应用示意图必须明确标注。
- Logo 周围至少保留相当于图形直径一半的安全空间，不放在复杂图片上。

## 7. 响应式规则

| 范围 | 规则 |
|---|---|
| ≥1200px | 12 列栅格；完整导航和超级菜单；Hero 可左右分栏 |
| 992–1199px | 保留桌面结构，减少导航间距；卡片通常三列 |
| 768–991px | 8 列栅格；导航转折叠模式；卡片两列 |
| ＜768px | 4 列栅格；Hero 纵向、文字在前；表单单列；卡片一至两列 |
| ＜480px | 单列优先；按钮可全宽；避免两个长西语按钮并排 |

- Hero 在移动端不得把必要文字叠在复杂图片上。
- 产品顺序固定为 PVC、WPC、UV、SPC。
- 表格在手机端优先转为纵向键值结构。
- 图片必须按设备提供合适尺寸，避免加载不必要的大图。
- 如使用移动端固定询价入口，不得遮挡正文、表单按钮或系统提示。

## 8. 动效与交互

- 常规过渡时间：160–240ms。
- 进入动画位移不超过 8–16px。
- 卡片悬停上移不超过 2–4px。
- 禁止自动轮播、全屏开场动画、视差滚动和自动播放有声视频。
- 不用动效承载必要信息。
- `prefers-reduced-motion: reduce` 下关闭位移、缩放、平滑滚动和非必要渐变动画。

## 9. 无障碍与可读性

- 普通文字对比度不低于 4.5:1；大字号文字不低于 3:1。
- 键盘焦点使用 3px 清晰轮廓和 2–3px 偏移，不能只改变颜色。
- 点击或触摸目标不小于 44×44px。
- 页面提供跳至正文链接。
- 导航、超级菜单、语言切换、FAQ 和表单可完整使用键盘操作。
- 菜单支持 Tab、Shift+Tab、方向键和 Escape。
- 表单错误不能只使用红色，必须同时提供文字说明。
- 西班牙语根页面使用 `lang="es"`，`/en/` 页面使用 `lang="en"`。
- 标题语义顺序必须连续，不因视觉字号而跳级。
- 不使用低对比度细字、全大写长段落或过长行宽。

## 10. 内容与证据禁用规则

- 不展示 CE、SGS、FloorScore、ISO 等没有独立原件支持的认证。
- 不使用“最高阻燃”“最高环保”“行业领先”等未证实的绝对化表述。
- 不把效果图、产品场景图或普通素材包装成真实客户案例。
- 不虚构客户名称、国家、合作品牌、项目结果或交付数据。
- 不设置客户 Logo 墙、认证墙或无证据的评价轮播。
- 不用生成图替代工厂、交付、认证和案例证据。
- 不使用自动播放轮播、连续弹窗、倒计时或过度动效。
- 不为了 SEO 在导航、Footer 或正文中堆叠关键词。
- 不在首页使用与 PVC 分类页完全相同的精确产品主题；首页坚持 SINOQI 品牌主题。
- 任何无法确认的参数、适用性或承诺必须标记待确认，不能自行补造。

## 11. 首页模块视觉顺序

本节仅规定后续页面实现必须遵循的结构，不代表本次已开始制作页面。

1. Utility Bar 与 Header：Logo、ES/EN、联系方式和唯一主 CTA。
2. Hero：品牌主题、供应能力、询价 CTA 和一张真实产品或工厂主图。
3. 可信事实条：1990、29,220㎡、110 名员工、全产品 300 × 40HQ/月。
4. 四大产品体系：PVC、WPC、UV、SPC 四张等权产品卡。
5. 适配客户：进口商、经销商、批发/连锁采购和工程采购。
6. Manufacturing：生产线、仓库、包装和装柜证据。
7. Applications：以应用示意帮助选型，不命名为案例。
8. 采购与合作条件：MOQ、交期、包装、样品和定制评估。
9. 合作流程：需求、报价、样品、确认、生产和交付。
10. FAQ：MOQ、样品、定制、包装、交期和询价准备。
11. 询盘转化区：一个工作日内响应、表单、邮箱和电话/WhatsApp。
12. Footer：产品、公司、Applications、FAQ、Contact 和联系方式。

首版首页不设置客户 Logo 墙、认证墙、案例成果区、新闻轮播、自动播放视频或大量 SKU 瀑布流。

## 12. 与现有设计系统和全局样式的衔接

项目当前没有独立组件库或设计 token 文件；现有实现主要由 `src/app/globals.css` 中的 CSS 自定义属性和组件类，以及 `src/components/` 下的 React 组件共同承担。Tailwind CSS 已被导入，但当前主要视觉规则仍是普通 CSS 类。

### 12.1 已经一致的部分

- `--green: #1f5a47` 与确认主色一致。
- `--orange: #e69a52` 与确认辅助色一致。
- `--green-pale: #eaf1ed` 与确认浅绿色一致。
- 正文已经使用 Inter 优先的无衬线字体。
- 容器当前为 1200px，符合 1200–1240px 规范。
- 已有 3px 焦点轮廓、跳至正文链接、移动端布局和减少动态偏好处理。
- Header、表单、FAQ、Footer 和产品卡已有可继续归一化的组件类。

### 12.2 后续实现前需要归一化的差异

本轮不修改以下代码；开始页面实现或视觉校正时，应以本文件为准统一处理：

- 将全站标题中的 Georgia/Times New Roman 迁移到统一无衬线字体。
- 将 H1 最大字号从现有 70–72px 收敛到 64px。
- 将深绿、中性色、边框色、暖白背景和橙色悬停值统一到本规范 token。
- 将普通按钮、输入框和卡片圆角统一为 4px。
- 将产品卡主图从接近 1:1 调整为 4:3。
- 将产品卡悬停位移从 7px 收敛至不超过 4px。
- 减少普通卡片的大阴影，仅为菜单、弹层和必要浮层保留轻阴影。
- 检查橙色按钮文字对比度，保持深色文字。
- 检查移动端固定或粘性元素，确保不遮挡表单和正文。
- 统一所有新组件使用上述 token，避免继续新增相近但不一致的硬编码色值。

### 12.3 执行优先级

1. 本文件中的已确认规范。
2. 已确认的开工基线、SEO 页面映射和禁写规则。
3. 现有 CSS 与组件实现。

若现有实现与本规范冲突，应在实际页面制作或视觉校正任务中调整实现，不应反向修改本规范来迁就旧样式。

## 13. 后续页面必须遵守的十条规则

1. 只使用确认的绿色、橙色和中性色体系，Logo 红色不扩展为 UI 色。
2. 标题和正文统一使用无衬线字体，遵守桌面与移动字号层级。
3. 全站使用 8px 间距体系、4px 常规圆角和克制阴影。
4. 同一屏幕通常只设置一个最高优先级 CTA。
5. 四条产品线的名称和顺序始终保持 PVC、WPC、UV、SPC。
6. 真实产品和工厂证据优先，不虚构认证、案例、客户或结果。
7. 产品图、工厂图、应用图和案例图严格按各自用途及比例使用。
8. 所有页面必须适配桌面、平板和手机，移动端不得照搬桌面排版。
9. 所有导航、按钮、表单和 FAQ 必须满足键盘、焦点、对比度和减少动态要求。
10. 禁止自动轮播、过度动效、重复 CTA、高密度导航和可见关键词堆叠。
